#Report.jsx 

Move the textarea in the buttom the result on top like chat layout...The system must be smart to generate in table 
like. 
i ask create data timetable about computer vision, it must make generate like there in the docx
the ai generate, at the end there is button we conver the whole response into a docs in the table in horizontal beautiful...
I’m going to implement this directly: update the UI to a chat-style layout (results on top, input fixed at bottom), add smarter timetable table rendering for prompts like your computer vision example, and add an export button to generate a .docx file with a horizontal, styled table. I’ll inspect the current React files first and then patch them.


DeepSeek.jsx
@Report.jsx



make sure the buttons must look like human created remove those spark icon which is star...



the layout must be like PRS OBE DOCX HERE